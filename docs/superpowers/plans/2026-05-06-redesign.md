# Chocolaterie Bugnon — Full Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete clean-slate rebuild of `landing/index.html` and `landing/css/styles.css` as a warm-light, single-page artisan chocolatier site that reads as a confident, finished piece — no demo text, no AI-template feel.

**Architecture:** Single HTML page with 9 anchor-linked sections, one CSS file using custom properties for the warm-light palette, and ~40 lines of vanilla JS for sticky nav and mobile burger menu. No build tools, no dependencies beyond Google Fonts.

**Tech Stack:** HTML5, CSS3 (custom properties + Grid + Flexbox), vanilla JavaScript, Google Fonts (Playfair Display + Inter)

**Design spec:** `docs/superpowers/specs/2026-05-06-redesign-design.md`

**Assets available in `landing/assets/`:**
- `bugnon-logo-user.png` — ornamental logo symbol (use as-is on light bg, `filter: invert(1) brightness(2)` on dark bg)
- `chocolate-hero.jpg` — hero/tile photo
- `chocolate-fountain.jpg` — events section photo

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `landing/index.html` | **Rewrite** | Full page HTML, all 9 sections + nav + footer |
| `landing/css/styles.css` | **Rewrite** | All styles: variables, reset, nav, sections, components, responsive |
| `landing/js/main.js` | **Update** | Sticky nav scroll class + mobile burger toggle |

---

## Task 1: CSS Foundation — Variables, Reset, Typography

**Files:**
- Rewrite: `landing/css/styles.css`

- [ ] **Step 1: Write the complete CSS foundation**

Replace the entire contents of `landing/css/styles.css` with:

```css
/* ===== VARIABLES ===== */
:root {
  --bg:          #faf6f0;
  --bg2:         #f3ece2;
  --bg-dark:     #3d1f0c;
  --bg-footer:   #2a1508;
  --text:        #2a1508;
  --text2:       #5a3d25;
  --text3:       #7a5c3c;
  --accent:      #9c6e3c;
  --accent-gold: #e8c98a;
  --cta:         #3d1f0c;
  --cta-text:    #faf6f0;
  --radius:      6px;
  --max:         1100px;
}

/* ===== RESET ===== */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: 'Inter', system-ui, sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
img { display: block; max-width: 100%; }
a { color: inherit; text-decoration: none; }

/* ===== TYPOGRAPHY ===== */
h1, h2, h3 {
  font-family: 'Playfair Display', Georgia, serif;
  font-style: italic;
  line-height: 1.2;
}
h1 { font-size: clamp(2rem, 5vw, 3rem); }
h2 { font-size: clamp(1.6rem, 3.5vw, 2.2rem); }
h3 { font-size: 1.15rem; font-style: normal; font-weight: 600; }

/* ===== LAYOUT HELPERS ===== */
.container { max-width: var(--max); margin: 0 auto; padding: 0 1.5rem; }
.section { padding: 5rem 0; }
.section--dark { background: var(--bg-dark); color: var(--cta-text); }
.section--alt  { background: var(--bg2); }

/* ===== DECORATIVE RULE ===== */
.rule {
  width: 28px; height: 2px;
  background: var(--accent);
  margin: 1rem 0;
}
.rule--center { margin: 1rem auto; }
.rule--gold { background: var(--accent-gold); }

/* ===== SECTION HEADING ===== */
.section-eyebrow {
  font-family: system-ui, sans-serif;
  font-size: 0.7rem;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 0.5rem;
}
.section-eyebrow--gold { color: var(--accent-gold); }

/* ===== BUTTONS ===== */
.btn {
  display: inline-block;
  font-family: system-ui, sans-serif;
  font-size: 0.8rem;
  letter-spacing: 0.05em;
  padding: 0.65rem 1.4rem;
  border-radius: var(--radius);
  border: 2px solid transparent;
  cursor: pointer;
  transition: opacity 0.15s, transform 0.15s;
  white-space: nowrap;
}
.btn:hover { opacity: 0.85; transform: translateY(-1px); }
.btn--primary   { background: var(--cta); color: var(--cta-text); border-color: var(--cta); }
.btn--outline   { background: transparent; color: var(--accent); border-color: var(--accent); }
.btn--outline-light { background: transparent; color: var(--cta-text); border-color: rgba(250,246,240,0.4); }
.btn--gold      { background: var(--accent-gold); color: var(--text); border-color: var(--accent-gold); }

/* ===== ACCESSIBILITY ===== */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}
:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; }
```

- [ ] **Step 2: Verify file saved correctly**

```bash
wc -l landing/css/styles.css
```
Expected: ~90 lines

- [ ] **Step 3: Commit**

```bash
git add landing/css/styles.css
git commit -m "feat: css foundation — variables, reset, typography, buttons"
```

---

## Task 2: HTML Skeleton + Navigation

**Files:**
- Rewrite: `landing/index.html`
- Append: `landing/css/styles.css`

- [ ] **Step 1: Write the full HTML skeleton with nav**

Replace `landing/index.html` entirely:

```html
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chocolaterie du Bugnon — Artisan chocolatier à Lausanne</title>
  <meta name="description" content="Chocolaterie artisanale à Lausanne depuis 2014. Chocolats grands crus, confiseries, glaces & sorbets, commandes sur mesure en 48h.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:ital,wght@0,600;1,400;1,600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>

  <a href="#main" class="sr-only">Aller au contenu principal</a>

  <!-- NAVIGATION -->
  <header class="nav" id="nav">
    <div class="container nav__inner">
      <a href="#" class="nav__brand" aria-label="Chocolaterie du Bugnon — accueil">
        <img src="assets/bugnon-logo-user.png" alt="" class="nav__logo" width="36" height="36">
        <div class="nav__wordmark">
          <span class="nav__name">Bugnon</span>
          <span class="nav__tagline">Chocolatier artisan</span>
        </div>
      </a>
      <nav aria-label="Navigation principale">
        <ul class="nav__links" id="nav-links">
          <li><a href="#produits">Produits</a></li>
          <li><a href="#sur-mesure">Sur mesure</a></li>
          <li><a href="#evenements">Événements</a></li>
          <li><a href="#contact">Contact</a></li>
          <li class="nav__cta-item"><a href="https://maps.google.com/?q=Rue+du+Bugnon+10+1005+Lausanne" class="btn btn--primary nav__cta" target="_blank" rel="noopener">Itinéraire ↗</a></li>
        </ul>
        <button class="nav__burger" id="nav-burger" aria-expanded="false" aria-controls="nav-links" aria-label="Menu">
          <span></span><span></span><span></span>
        </button>
      </nav>
    </div>
  </header>

  <main id="main">

    <!-- HERO -->
    <section class="hero section" id="accueil">
      <div class="container hero__inner"><!-- Task 3 --></div>
    </section>

    <!-- BANDEAU PREUVES -->
    <section class="bandeau section--dark" aria-label="Nos engagements">
      <div class="container bandeau__inner"><!-- Task 4 --></div>
    </section>

    <!-- VALEURS -->
    <section class="valeurs section" id="valeurs">
      <div class="container valeurs__inner"><!-- Task 5 --></div>
    </section>

    <!-- PRODUITS -->
    <section class="produits section section--alt" id="produits">
      <div class="container produits__inner"><!-- Task 6 --></div>
    </section>

    <!-- CAMPAGNE SAISONNIÈRE -->
    <section class="campagne section section--dark" id="saison">
      <div class="container campagne__inner"><!-- Task 7 --></div>
    </section>

    <!-- SUR MESURE -->
    <section class="sur-mesure section" id="sur-mesure">
      <div class="container sur-mesure__inner"><!-- Task 8 --></div>
    </section>

    <!-- ÉVÉNEMENTS -->
    <section class="evenements section section--alt" id="evenements">
      <div class="container evenements__inner"><!-- Task 9 --></div>
    </section>

    <!-- AVIS -->
    <section class="avis section" id="avis">
      <div class="container avis__inner"><!-- Task 10 --></div>
    </section>

    <!-- ACCÈS & CONTACT -->
    <section class="acces section section--alt" id="contact">
      <div class="container acces__inner"><!-- Task 11 --></div>
    </section>

  </main>

  <!-- FOOTER -->
  <footer class="footer" id="footer">
    <div class="container footer__inner"><!-- Task 12 --></div>
  </footer>

  <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: Add nav CSS to `landing/css/styles.css`**

Append to end of `landing/css/styles.css`:

```css
/* ===== NAVIGATION ===== */
.nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 100;
  background: var(--bg);
  border-bottom: 1px solid rgba(61,31,12,0.1);
  transition: box-shadow 0.2s, backdrop-filter 0.2s;
}
.nav.scrolled {
  box-shadow: 0 2px 16px rgba(61,31,12,0.12);
  backdrop-filter: blur(8px);
  background: rgba(250,246,240,0.92);
}
.nav__inner {
  display: flex; align-items: center; justify-content: space-between;
  height: 68px;
}
.nav__brand {
  display: flex; align-items: center; gap: 0.75rem;
}
.nav__logo { width: 36px; height: 36px; object-fit: contain; }
.nav__wordmark { display: flex; flex-direction: column; }
.nav__name {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 1.1rem; font-weight: 600; font-style: italic;
  color: var(--text); line-height: 1;
}
.nav__tagline {
  font-family: system-ui, sans-serif;
  font-size: 0.6rem; letter-spacing: 0.2em;
  text-transform: uppercase; color: var(--accent);
}
.nav__links {
  display: flex; align-items: center; gap: 2rem; list-style: none;
}
.nav__links a {
  font-family: system-ui, sans-serif; font-size: 0.85rem;
  color: var(--text3); transition: color 0.15s;
}
.nav__links a:hover { color: var(--text); }
.nav__cta-item { margin-left: 0.5rem; }
.nav__burger {
  display: none; flex-direction: column; gap: 5px;
  background: none; border: none; cursor: pointer; padding: 4px;
}
.nav__burger span {
  display: block; width: 22px; height: 2px;
  background: var(--text); border-radius: 2px;
  transition: transform 0.2s, opacity 0.2s;
}
```

- [ ] **Step 3: Open in browser and verify**

```bash
open landing/index.html
```
Expected: Parchment-coloured page, nav visible at top with logo + "Bugnon" wordmark + nav links + Itinéraire button. Page has no content below nav yet (all sections empty).

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: html skeleton + sticky nav with logo"
```

---

## Task 3: Hero Section

**Files:**
- Modify: `landing/index.html` (hero section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill hero HTML**

Replace `<!-- Task 3 -->` inside `.hero__inner` with:

```html
<div class="hero__text">
  <p class="section-eyebrow">Artisan chocolatier · Lausanne · Depuis 2014</p>
  <h1>Chaque chocolat<br>raconte une histoire</h1>
  <div class="rule"></div>
  <p class="hero__sub">Confectionnés à la main à Lausanne. Grands crus, créations de saison, dégustations en boutique.</p>
  <div class="hero__actions">
    <a href="https://maps.google.com/?q=Rue+du+Bugnon+10+1005+Lausanne" class="btn btn--primary" target="_blank" rel="noopener">Itinéraire</a>
    <a href="#sur-mesure" class="btn btn--outline">Sur mesure (48h)</a>
  </div>
  <p class="hero__address">📍 Rue du Bugnon 10 · 1005 Lausanne</p>
</div>
<div class="hero__media">
  <img src="assets/chocolate-hero.jpg" alt="Chocolats artisanaux de la Chocolaterie du Bugnon" class="hero__img">
</div>
```

- [ ] **Step 2: Add hero CSS**

Append to `landing/css/styles.css`:

```css
/* ===== HERO ===== */
.hero { padding-top: calc(68px + 4rem); }
.hero__inner {
  display: grid;
  grid-template-columns: 55fr 45fr;
  gap: 3rem; align-items: center;
}
.hero__text { display: flex; flex-direction: column; }
.hero__sub {
  color: var(--text2); font-size: 1rem; line-height: 1.7;
  margin-bottom: 2rem; max-width: 38ch;
}
.hero__actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-bottom: 1.5rem; }
.hero__address {
  font-family: system-ui, sans-serif; font-size: 0.8rem;
  color: var(--accent); letter-spacing: 0.04em;
}
.hero__media { position: relative; }
.hero__img {
  width: 100%; height: 480px; object-fit: cover;
  border-radius: 8px;
  box-shadow: 0 8px 32px rgba(61,31,12,0.15);
}
```

- [ ] **Step 3: Verify in browser**

Refresh `landing/index.html`. Expected: H1 heading italic serif, amber eyebrow line, thin amber rule, two CTA buttons, address line. Photo fills the right column.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: hero section — split layout, H1, CTAs, photo"
```

---

## Task 4: Bandeau Preuves

**Files:**
- Modify: `landing/index.html` (bandeau section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill bandeau HTML**

Replace `<!-- Task 4 -->` inside `.bandeau__inner` with:

```html
<ul class="bandeau__list">
  <li class="bandeau__item">
    <span class="bandeau__stat">2014</span>
    <span class="bandeau__desc">En boutique<br>à Lausanne</span>
  </li>
  <li class="bandeau__item">
    <span class="bandeau__stat">10+</span>
    <span class="bandeau__desc">Origines<br>de cacao</span>
  </li>
  <li class="bandeau__item">
    <span class="bandeau__stat">100%</span>
    <span class="bandeau__desc">Beurre de cacao<br>&amp; vanille pure</span>
  </li>
  <li class="bandeau__item">
    <span class="bandeau__stat">48h</span>
    <span class="bandeau__desc">Délai sur<br>mesure</span>
  </li>
</ul>
```

- [ ] **Step 2: Add bandeau CSS**

Append to `landing/css/styles.css`:

```css
/* ===== BANDEAU PREUVES ===== */
.bandeau { padding: 3rem 0; }
.bandeau__list {
  display: grid; grid-template-columns: repeat(4, 1fr);
  gap: 1rem; list-style: none; text-align: center;
}
.bandeau__item {
  display: flex; flex-direction: column; align-items: center; gap: 0.4rem;
  padding: 1rem;
  border-right: 1px solid rgba(250,246,240,0.1);
}
.bandeau__item:last-child { border-right: none; }
.bandeau__stat {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 2rem; font-style: italic; font-weight: 400;
  color: var(--accent-gold);
}
.bandeau__desc {
  font-family: system-ui, sans-serif; font-size: 0.78rem;
  color: rgba(250,246,240,0.75); line-height: 1.5; text-align: center;
}
```

- [ ] **Step 3: Verify in browser**

Scroll down past hero. Expected: dark brown band with 4 stats in italic gold (2014 / 10+ / 100% / 48h), cream descriptions below each, thin dividers between items.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: bandeau preuves — 4 USP stats on dark background"
```

---

## Task 5: Nos Valeurs

**Files:**
- Modify: `landing/index.html` (valeurs section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill valeurs HTML**

Replace `<!-- Task 5 -->` inside `.valeurs__inner` with:

```html
<div class="section-header section-header--center">
  <p class="section-eyebrow">Notre philosophie</p>
  <h2>L'artisan, le goût, l'émotion</h2>
  <div class="rule rule--center"></div>
  <p class="section-sub">Trois engagements au cœur de chaque création.</p>
</div>
<ul class="valeurs__list">
  <li class="valeurs__card">
    <span class="valeurs__icon" aria-hidden="true">✦</span>
    <h3>Artisanat</h3>
    <p>Chaque création façonnée à la main avec soin, dans notre atelier lausannois.</p>
  </li>
  <li class="valeurs__card">
    <span class="valeurs__icon" aria-hidden="true">◈</span>
    <h3>Qualité</h3>
    <p>Grands crus sélectionnés, arômes francs et naturels, sans colorants ni conservateurs.</p>
  </li>
  <li class="valeurs__card">
    <span class="valeurs__icon" aria-hidden="true">♡</span>
    <h3>Émotion</h3>
    <p>Goût, partage, raffinement — chaque chocolat est conçu pour créer un moment.</p>
  </li>
</ul>
```

- [ ] **Step 2: Add valeurs CSS**

Append to `landing/css/styles.css`:

```css
/* ===== SECTION HEADER (reusable) ===== */
.section-header { margin-bottom: 3rem; }
.section-header--center { text-align: center; }
.section-sub {
  color: var(--text3); font-family: system-ui, sans-serif;
  font-size: 0.95rem; max-width: 46ch; line-height: 1.7;
}
.section-header--center .section-sub { margin: 0 auto; }

/* ===== VALEURS ===== */
.valeurs__list {
  display: grid; grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem; list-style: none;
}
.valeurs__card {
  background: var(--bg2); border-radius: 8px;
  padding: 2rem 1.75rem;
  display: flex; flex-direction: column; gap: 0.75rem;
  transition: box-shadow 0.2s;
}
.valeurs__card:hover { box-shadow: 0 4px 20px rgba(61,31,12,0.08); }
.valeurs__icon { font-size: 1.4rem; color: var(--accent); }
.valeurs__card h3 { font-style: italic; color: var(--text); }
.valeurs__card p {
  font-family: system-ui, sans-serif; font-size: 0.9rem;
  color: var(--text2); line-height: 1.65;
}
```

- [ ] **Step 3: Verify in browser**

Expected: 3 warm-cream cards on parchment background, symbol + bold title + description each, centered section heading with rule above.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: valeurs section — 3 value cards"
```

---

## Task 6: Nos Produits

**Files:**
- Modify: `landing/index.html` (produits section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill produits HTML**

Replace `<!-- Task 6 -->` inside `.produits__inner` with:

```html
<div class="section-header section-header--center">
  <p class="section-eyebrow">La boutique</p>
  <h2>Découvrir nos créations</h2>
  <div class="rule rule--center"></div>
</div>
<ul class="produits__grid">
  <li class="produits__tile">
    <div class="produits__img-wrap">
      <img src="assets/chocolate-hero.jpg" alt="Chocolats artisanaux" class="produits__img">
    </div>
    <span class="produits__label">Chocolats</span>
  </li>
  <li class="produits__tile">
    <div class="produits__img-wrap">
      <img src="assets/chocolate-hero.jpg" alt="Confiseries artisanales" class="produits__img">
    </div>
    <span class="produits__label">Confiseries</span>
  </li>
  <li class="produits__tile">
    <div class="produits__img-wrap">
      <img src="assets/chocolate-hero.jpg" alt="Glaces artisanales" class="produits__img">
    </div>
    <span class="produits__label">Glaces</span>
  </li>
  <li class="produits__tile">
    <div class="produits__img-wrap">
      <img src="assets/chocolate-hero.jpg" alt="Sorbets artisanaux" class="produits__img">
    </div>
    <span class="produits__label">Sorbets</span>
  </li>
  <li class="produits__tile produits__tile--saison">
    <div class="produits__img-wrap">
      <img src="assets/chocolate-hero.jpg" alt="Créations saisonnières" class="produits__img">
    </div>
    <span class="produits__label">Saisonnier ✦</span>
  </li>
</ul>
```

- [ ] **Step 2: Add produits CSS**

Append to `landing/css/styles.css`:

```css
/* ===== PRODUITS ===== */
.produits__grid {
  display: grid; grid-template-columns: repeat(5, 1fr);
  gap: 1rem; list-style: none;
}
.produits__tile {
  display: flex; flex-direction: column; gap: 0.6rem;
  cursor: pointer;
}
.produits__img-wrap {
  overflow: hidden; border-radius: var(--radius);
  aspect-ratio: 1 / 1;
}
.produits__img {
  width: 100%; height: 100%; object-fit: cover;
  transition: transform 0.35s ease;
}
.produits__tile:hover .produits__img { transform: scale(1.05); }
.produits__label {
  font-family: system-ui, sans-serif; font-size: 0.82rem;
  font-weight: 600; color: var(--text);
  text-align: center; letter-spacing: 0.02em;
  transition: color 0.2s;
}
.produits__tile:hover .produits__label { color: var(--accent); }
.produits__tile--saison .produits__label { color: var(--accent); }
.produits__tile--saison .produits__img-wrap {
  outline: 2px solid var(--accent); outline-offset: 3px;
}
```

- [ ] **Step 3: Verify in browser**

Expected: 5 square tiles in a row on the alt background, same photo for now (placeholder), subtle zoom on hover, Saisonnier tile has amber outline.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: produits section — 5-tile category grid"
```

---

## Task 7: Campagne Saisonnière

**Files:**
- Modify: `landing/index.html` (campagne section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill campagne HTML**

Replace `<!-- Task 7 -->` inside `.campagne__inner` with:

```html
<div class="campagne__visual">
  <img src="assets/chocolate-hero.jpg" alt="Chocolats de Pâques artisanaux" class="campagne__img">
</div>
<div class="campagne__text">
  <p class="section-eyebrow section-eyebrow--gold">Pâques 2025</p>
  <h2 style="color: var(--cta-text);">Offrez une Pâques gourmande</h2>
  <div class="rule rule--gold"></div>
  <p class="campagne__sub">Œufs en chocolat artisanal, le cadeau idéal pour célébrer ensemble. Création sur mesure disponible en 48h.</p>
  <div class="campagne__actions">
    <a href="#produits" class="btn btn--gold">Découvrir la collection</a>
    <a href="#sur-mesure" class="btn btn--outline-light">Commander sur mesure</a>
  </div>
</div>
```

- [ ] **Step 2: Add campagne CSS**

Append to `landing/css/styles.css`:

```css
/* ===== CAMPAGNE SAISONNIÈRE ===== */
.campagne__inner {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 3.5rem; align-items: center;
}
.campagne__img {
  width: 100%; height: 380px; object-fit: cover;
  border-radius: 8px;
  box-shadow: 0 8px 40px rgba(0,0,0,0.3);
}
.campagne__sub {
  color: rgba(250,246,240,0.8); font-family: system-ui, sans-serif;
  font-size: 0.95rem; line-height: 1.7;
  margin-bottom: 2rem; max-width: 38ch;
}
.campagne__actions { display: flex; gap: 1rem; flex-wrap: wrap; }
```

- [ ] **Step 3: Verify in browser**

Expected: dark brown section, photo left, italic cream heading right, gold eyebrow, gold rule, two CTA buttons.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: campagne saisonnière section — seasonal dark feature"
```

---

## Task 8: Sur Mesure

**Files:**
- Modify: `landing/index.html` (sur-mesure section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill sur-mesure HTML**

Replace `<!-- Task 8 -->` inside `.sur-mesure__inner` with:

```html
<div class="sur-mesure__pitch">
  <p class="section-eyebrow">Commandes personnalisées</p>
  <h2>Une création<br>à votre image</h2>
  <div class="rule"></div>
  <p class="sur-mesure__sub">Entreprises, anniversaires, mariages. Nous réalisons vos envies avec soin, retrait en boutique sous 48h minimum.</p>
  <ul class="sur-mesure__usages">
    <li><span aria-hidden="true">◉</span> Cadeau d'entreprise</li>
    <li><span aria-hidden="true">◉</span> Anniversaire &amp; mariage</li>
    <li><span aria-hidden="true">◉</span> Cadeau personnalisé</li>
  </ul>
  <a href="#sur-mesure-form" class="btn btn--primary">Faire une demande</a>
</div>
<div class="sur-mesure__form-wrap" id="sur-mesure-form">
  <form class="sur-mesure__form" name="sur-mesure" method="POST" novalidate>
    <p class="section-eyebrow" style="margin-bottom:1.25rem;">Formulaire de demande</p>
    <div class="form-group">
      <label for="sm-nom">Nom &amp; prénom</label>
      <input type="text" id="sm-nom" name="nom" placeholder="Marie Dupont" autocomplete="name">
    </div>
    <div class="form-group">
      <label for="sm-contact">Email ou téléphone</label>
      <input type="text" id="sm-contact" name="contact" placeholder="marie@exemple.ch" autocomplete="email">
    </div>
    <div class="form-group">
      <label for="sm-type">Type de demande</label>
      <select id="sm-type" name="type">
        <option value="">Choisir...</option>
        <option value="entreprise">Cadeau d'entreprise</option>
        <option value="anniversaire">Anniversaire</option>
        <option value="mariage">Mariage</option>
        <option value="cadeau">Cadeau personnel</option>
        <option value="autre">Autre</option>
      </select>
    </div>
    <div class="form-group">
      <label for="sm-message">Votre demande</label>
      <textarea id="sm-message" name="message" rows="4" placeholder="Décrivez votre idée, la quantité souhaitée, la date..."></textarea>
    </div>
    <button type="submit" class="btn btn--primary" style="width:100%;">Envoyer ma demande</button>
  </form>
</div>
```

- [ ] **Step 2: Add sur-mesure CSS**

Append to `landing/css/styles.css`:

```css
/* ===== SUR MESURE ===== */
.sur-mesure__inner {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 4rem; align-items: start;
}
.sur-mesure__sub {
  color: var(--text2); font-family: system-ui, sans-serif;
  font-size: 0.95rem; line-height: 1.7; margin-bottom: 1.5rem; max-width: 38ch;
}
.sur-mesure__usages {
  list-style: none; margin-bottom: 2rem;
  display: flex; flex-direction: column; gap: 0.6rem;
}
.sur-mesure__usages li {
  font-family: system-ui, sans-serif; font-size: 0.9rem;
  color: var(--text2); display: flex; align-items: center; gap: 0.6rem;
}
.sur-mesure__usages span { color: var(--accent); }
.sur-mesure__form-wrap {
  background: var(--bg2); border-radius: 10px; padding: 2rem;
}
.form-group {
  display: flex; flex-direction: column; gap: 0.4rem; margin-bottom: 1rem;
}
.form-group label {
  font-family: system-ui, sans-serif; font-size: 0.78rem;
  font-weight: 600; color: var(--text); letter-spacing: 0.02em;
}
.form-group input,
.form-group select,
.form-group textarea {
  font-family: system-ui, sans-serif; font-size: 0.9rem;
  color: var(--text); background: var(--bg);
  border: 1px solid rgba(61,31,12,0.2); border-radius: var(--radius);
  padding: 0.6rem 0.9rem; transition: border-color 0.15s;
  outline: none; resize: vertical;
}
.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(156,110,60,0.12);
}
```

- [ ] **Step 3: Verify in browser**

Expected: 2-col layout — pitch with 3 bullets left, styled form on warm-cream card right. No "(démo)" text anywhere. Form fields have focus states.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: sur mesure section — pitch + custom order form"
```

---

## Task 9: Événements

**Files:**
- Modify: `landing/index.html` (evenements section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill evenements HTML**

Replace `<!-- Task 9 -->` inside `.evenements__inner` with:

```html
<div class="evenements__text">
  <p class="section-eyebrow">Réceptions &amp; entreprises</p>
  <h2>La fontaine chocolat<br>pour vos événements</h2>
  <div class="rule"></div>
  <p class="evenements__sub">Mariages, soirées d'entreprise, anniversaires. Nous livrons et installons notre fontaine chocolat de 80 cm pour un moment inoubliable.</p>
  <ul class="evenements__specs">
    <li><span class="spec-tag">80 cm de hauteur</span></li>
    <li><span class="spec-tag">150 CHF / jour</span></li>
    <li><span class="spec-tag">Caution 600 CHF</span></li>
  </ul>
  <a href="#sur-mesure" class="btn btn--outline">Demander un devis</a>
</div>
<div class="evenements__media">
  <img src="assets/chocolate-fountain.jpg" alt="Fontaine chocolat pour événements" class="evenements__img">
</div>
```

- [ ] **Step 2: Add evenements CSS**

Append to `landing/css/styles.css`:

```css
/* ===== ÉVÉNEMENTS ===== */
.evenements__inner {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 3.5rem; align-items: center;
}
.evenements__sub {
  color: var(--text2); font-family: system-ui, sans-serif;
  font-size: 0.95rem; line-height: 1.7; margin-bottom: 1.5rem; max-width: 40ch;
}
.evenements__specs {
  list-style: none; display: flex; flex-wrap: wrap;
  gap: 0.5rem; margin-bottom: 1.75rem;
}
.spec-tag {
  background: rgba(61,31,12,0.08); border: 1px solid rgba(61,31,12,0.15);
  border-radius: 4px; padding: 0.3rem 0.75rem;
  font-family: system-ui, sans-serif; font-size: 0.8rem; color: var(--text2);
}
.evenements__img {
  width: 100%; height: 380px; object-fit: cover;
  border-radius: 8px; box-shadow: 0 8px 32px rgba(61,31,12,0.12);
}
```

- [ ] **Step 3: Verify in browser**

Expected: alt-bg section, text left with spec tags (80cm / 150CHF / caution), fountain photo right.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: evenements section — chocolate fountain details"
```

---

## Task 10: Avis Clients

**Files:**
- Modify: `landing/index.html` (avis section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill avis HTML**

Replace `<!-- Task 10 -->` inside `.avis__inner` with:

```html
<div class="section-header section-header--center">
  <p class="section-eyebrow">Ce qu'ils disent</p>
  <h2>Ils en parlent</h2>
  <div class="avis__stars" aria-label="5 étoiles">★★★★★</div>
</div>
<ul class="avis__list">
  <li class="avis__card">
    <blockquote>
      <p>"Des chocolats d'une finesse extraordinaire. Une adresse incontournable à Lausanne."</p>
      <footer><cite>— Marie L.</cite></footer>
    </blockquote>
  </li>
  <li class="avis__card">
    <blockquote>
      <p>"Commande sur mesure pour notre mariage, un résultat magnifique et des saveurs inoubliables."</p>
      <footer><cite>— Thomas &amp; Camille</cite></footer>
    </blockquote>
  </li>
  <li class="avis__card">
    <blockquote>
      <p>"La fontaine chocolat pour notre soirée d'entreprise — tout le monde était conquis. Merci !"</p>
      <footer><cite>— Sophie M., Directrice RH</cite></footer>
    </blockquote>
  </li>
</ul>
<div style="text-align:center; margin-top: 2.5rem;">
  <a href="https://maps.google.com/?q=Chocolaterie+du+Bugnon+Lausanne" class="btn btn--outline" target="_blank" rel="noopener">Voir tous les avis Google →</a>
</div>
```

- [ ] **Step 2: Add avis CSS**

Append to `landing/css/styles.css`:

```css
/* ===== AVIS ===== */
.avis__stars {
  color: var(--accent-gold); font-size: 1.1rem;
  letter-spacing: 0.15em; margin: 0.75rem 0 2.5rem;
}
.avis__list {
  display: grid; grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem; list-style: none;
}
.avis__card {
  background: var(--bg2); border-radius: 8px; padding: 1.75rem;
  border-left: 3px solid var(--accent);
}
.avis__card blockquote p {
  font-family: 'Playfair Display', Georgia, serif;
  font-style: italic; font-size: 0.95rem;
  color: var(--text2); line-height: 1.7;
  margin-bottom: 1rem;
}
.avis__card cite {
  font-family: system-ui, sans-serif; font-size: 0.8rem;
  font-style: normal; font-weight: 600; color: var(--accent);
}
```

- [ ] **Step 3: Verify in browser**

Expected: 3 cards with amber left border, italic quotes in Playfair, amber author names. Gold stars above.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: avis section — 3 testimonial cards"
```

---

## Task 11: Accès & Contact

**Files:**
- Modify: `landing/index.html` (acces section)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill accès HTML**

Replace `<!-- Task 11 -->` inside `.acces__inner` with:

```html
<div class="acces__info">
  <p class="section-eyebrow">Venir nous voir</p>
  <h2>Venez nous<br>rendre visite</h2>
  <div class="rule"></div>
  <dl class="acces__details">
    <div class="acces__row">
      <dt class="sr-only">Adresse</dt>
      <dd>📍 Rue du Bugnon 10, 1005 Lausanne</dd>
    </div>
    <div class="acces__row">
      <dt class="sr-only">Téléphone</dt>
      <dd>📞 <a href="tel:+41215583807">+41 21 558 38 07</a></dd>
    </div>
    <div class="acces__row">
      <dt class="sr-only">Horaires</dt>
      <dd>🕐 Lun–Ven 9h–18h30 &nbsp;·&nbsp; Sam 9h–17h</dd>
    </div>
  </dl>
  <div class="acces__actions">
    <a href="https://maps.google.com/?q=Rue+du+Bugnon+10+1005+Lausanne" class="btn btn--primary" target="_blank" rel="noopener">Itinéraire ↗</a>
    <a href="tel:+41215583807" class="btn btn--outline">Appeler</a>
  </div>
</div>
<div class="acces__map">
  <a href="https://maps.google.com/?q=Rue+du+Bugnon+10+1005+Lausanne" class="acces__map-link" target="_blank" rel="noopener" aria-label="Voir sur Google Maps">
    <div class="acces__map-placeholder">
      <span>Rue du Bugnon 10<br>1005 Lausanne</span>
      <span class="acces__map-cta">Voir sur Google Maps ↗</span>
    </div>
  </a>
</div>
```

- [ ] **Step 2: Add accès CSS**

Append to `landing/css/styles.css`:

```css
/* ===== ACCÈS & CONTACT ===== */
.acces__inner {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 3.5rem; align-items: center;
}
.acces__details {
  margin: 1.5rem 0; display: flex; flex-direction: column; gap: 0.75rem;
}
.acces__row dd {
  font-family: system-ui, sans-serif; font-size: 0.95rem;
  color: var(--text2); line-height: 1.6;
}
.acces__row a { color: var(--accent); }
.acces__row a:hover { text-decoration: underline; }
.acces__actions { display: flex; gap: 1rem; }
.acces__map-link { display: block; border-radius: 8px; overflow: hidden; height: 100%; }
.acces__map-placeholder {
  background: linear-gradient(135deg, #c4d4b0 0%, #8aab70 100%);
  height: 280px; border-radius: 8px;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: 0.75rem; text-align: center;
  transition: filter 0.2s;
}
.acces__map-placeholder:hover { filter: brightness(0.92); }
.acces__map-placeholder span {
  font-family: system-ui, sans-serif; font-size: 0.9rem;
  color: #2a3d1a; font-weight: 600; line-height: 1.5;
}
.acces__map-cta {
  font-size: 0.78rem !important; font-weight: 400 !important;
  color: #3a5a20 !important; border-bottom: 1px solid #3a5a2055;
}
```

- [ ] **Step 3: Verify in browser**

Expected: address, phone (click-to-call link), hours, two action buttons, green map placeholder that links to Google Maps on click.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: acces section — address, hours, CTA buttons, map placeholder"
```

---

## Task 12: Footer

**Files:**
- Modify: `landing/index.html` (footer)
- Append: `landing/css/styles.css`

- [ ] **Step 1: Fill footer HTML**

Replace `<!-- Task 12 -->` inside `.footer__inner` with:

```html
<div class="footer__grid">
  <div class="footer__brand">
    <div class="footer__brand-logo">
      <img src="assets/bugnon-logo-user.png" alt="" class="footer__logo" width="32" height="32">
      <span class="footer__name">Bugnon</span>
    </div>
    <address class="footer__address">
      Rue du Bugnon 10<br>
      1005 Lausanne<br>
      <a href="tel:+41215583807">+41 21 558 38 07</a>
    </address>
    <div class="footer__social">
      <a href="https://instagram.com" class="footer__social-link" target="_blank" rel="noopener">Instagram</a>
      <a href="https://facebook.com" class="footer__social-link" target="_blank" rel="noopener">Facebook</a>
    </div>
  </div>
  <nav class="footer__nav" aria-label="Navigation footer">
    <p class="footer__nav-title">Navigation</p>
    <ul>
      <li><a href="#produits">Produits</a></li>
      <li><a href="#sur-mesure">Sur mesure</a></li>
      <li><a href="#evenements">Événements</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </nav>
  <div class="footer__newsletter">
    <p class="footer__nav-title">Newsletter</p>
    <p class="footer__newsletter-sub">Nouveautés &amp; créations de saison.</p>
    <form class="footer__form" name="newsletter" method="POST" novalidate>
      <input type="email" name="email" placeholder="Votre email" aria-label="Adresse email" class="footer__input">
      <button type="submit" class="btn btn--gold footer__submit">S'inscrire</button>
    </form>
  </div>
</div>
<div class="footer__bottom">
  <span>© 2025 Chocolaterie du Bugnon</span>
  <span>Lausanne · Suisse</span>
</div>
```

- [ ] **Step 2: Add footer CSS**

Append to `landing/css/styles.css`:

```css
/* ===== FOOTER ===== */
.footer {
  background: var(--bg-footer);
  padding: 4rem 0 0;
}
.footer__grid {
  display: grid; grid-template-columns: 2fr 1fr 1.5fr;
  gap: 3rem; padding-bottom: 3rem;
  border-bottom: 1px solid rgba(250,246,240,0.08);
}
.footer__brand-logo {
  display: flex; align-items: center; gap: 0.6rem; margin-bottom: 1rem;
}
.footer__logo {
  width: 32px; height: 32px; object-fit: contain;
  filter: invert(1) brightness(2);
}
.footer__name {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 1.1rem; font-style: italic; color: var(--cta-text);
}
.footer__address {
  font-family: system-ui, sans-serif; font-size: 0.82rem;
  color: rgba(250,246,240,0.6); line-height: 1.9; font-style: normal;
  margin-bottom: 1.25rem;
}
.footer__address a { color: rgba(250,246,240,0.6); }
.footer__address a:hover { color: var(--cta-text); }
.footer__social { display: flex; gap: 0.5rem; }
.footer__social-link {
  font-family: system-ui, sans-serif; font-size: 0.75rem;
  background: rgba(250,246,240,0.08); border: 1px solid rgba(250,246,240,0.15);
  color: rgba(250,246,240,0.7); padding: 0.3rem 0.8rem;
  border-radius: 4px; transition: background 0.15s;
}
.footer__social-link:hover { background: rgba(250,246,240,0.15); color: var(--cta-text); }
.footer__nav-title {
  font-family: system-ui, sans-serif; font-size: 0.7rem;
  letter-spacing: 0.2em; text-transform: uppercase;
  color: var(--accent-gold); margin-bottom: 1rem;
}
.footer__nav ul { list-style: none; display: flex; flex-direction: column; gap: 0.6rem; }
.footer__nav a {
  font-family: system-ui, sans-serif; font-size: 0.88rem;
  color: rgba(250,246,240,0.65); transition: color 0.15s;
}
.footer__nav a:hover { color: var(--cta-text); }
.footer__newsletter-sub {
  font-family: system-ui, sans-serif; font-size: 0.82rem;
  color: rgba(250,246,240,0.6); margin-bottom: 1rem;
}
.footer__form { display: flex; flex-direction: column; gap: 0.6rem; }
.footer__input {
  font-family: system-ui, sans-serif; font-size: 0.88rem;
  background: rgba(250,246,240,0.07); border: 1px solid rgba(250,246,240,0.15);
  color: var(--cta-text); border-radius: var(--radius); padding: 0.6rem 0.9rem;
  outline: none; transition: border-color 0.15s;
}
.footer__input::placeholder { color: rgba(250,246,240,0.35); }
.footer__input:focus { border-color: var(--accent-gold); }
.footer__submit { width: 100%; }
.footer__bottom {
  display: flex; justify-content: space-between; align-items: center;
  padding: 1.25rem 0;
  font-family: system-ui, sans-serif; font-size: 0.75rem;
  color: rgba(250,246,240,0.3);
}
```

- [ ] **Step 3: Verify in browser**

Expected: very dark footer, logo inverted to cream, nav links in 3 columns, newsletter input + gold subscribe button. Bottom bar with copyright.

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: footer — brand, nav, newsletter, bottom bar"
```

---

## Task 13: JavaScript — Sticky Nav + Mobile Burger

**Files:**
- Rewrite: `landing/js/main.js`

- [ ] **Step 1: Write main.js**

Replace `landing/js/main.js` entirely:

```javascript
(function () {
  const nav = document.getElementById('nav');
  const burger = document.getElementById('nav-burger');
  const navLinks = document.getElementById('nav-links');

  // Sticky nav: add .scrolled class when page scrolls
  window.addEventListener('scroll', function () {
    nav.classList.toggle('scrolled', window.scrollY > 20);
  }, { passive: true });

  // Mobile burger toggle
  burger.addEventListener('click', function () {
    const isOpen = navLinks.classList.toggle('nav__links--open');
    burger.setAttribute('aria-expanded', String(isOpen));
    burger.classList.toggle('nav__burger--open', isOpen);
  });

  // Close menu when a nav link is clicked
  navLinks.addEventListener('click', function (e) {
    if (e.target.tagName === 'A') {
      navLinks.classList.remove('nav__links--open');
      burger.setAttribute('aria-expanded', 'false');
      burger.classList.remove('nav__burger--open');
    }
  });
})();
```

- [ ] **Step 2: Add mobile nav CSS**

Append to `landing/css/styles.css`:

```css
/* ===== MOBILE NAV OPEN STATE ===== */
.nav__burger--open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
.nav__burger--open span:nth-child(2) { opacity: 0; }
.nav__burger--open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
```

- [ ] **Step 3: Verify scroll + burger**

Open in browser. Scroll down — nav should get a shadow. Narrow window to < 980px — burger should appear. Click burger — menu should open/close.

- [ ] **Step 4: Commit**

```bash
git add landing/js/main.js landing/css/styles.css
git commit -m "feat: js — sticky nav scroll class + mobile burger toggle"
```

---

## Task 14: Responsive — Mobile Breakpoints

**Files:**
- Append: `landing/css/styles.css`

- [ ] **Step 1: Add all responsive rules**

Append to `landing/css/styles.css`:

```css
/* ===== RESPONSIVE (< 980px) ===== */
@media (max-width: 980px) {

  /* Nav */
  .nav__links {
    display: none; flex-direction: column;
    position: absolute; top: 68px; left: 0; right: 0;
    background: var(--bg); border-bottom: 1px solid rgba(61,31,12,0.1);
    padding: 1.5rem; gap: 1.25rem;
    box-shadow: 0 8px 24px rgba(61,31,12,0.1);
  }
  .nav__links--open { display: flex; }
  .nav__burger { display: flex; }
  .nav__cta-item { margin-left: 0; }

  /* Hero */
  .hero__inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .hero__img { height: 280px; }
  .hero { padding-top: calc(68px + 2.5rem); }

  /* Bandeau */
  .bandeau__list { grid-template-columns: repeat(2, 1fr); }
  .bandeau__item:nth-child(2) { border-right: none; }
  .bandeau__item { border-bottom: 1px solid rgba(250,246,240,0.1); }
  .bandeau__item:nth-child(3),
  .bandeau__item:nth-child(4) { border-bottom: none; }

  /* Valeurs */
  .valeurs__list { grid-template-columns: 1fr; }

  /* Produits */
  .produits__grid { grid-template-columns: repeat(2, 1fr); }

  /* Campagne */
  .campagne__inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .campagne__img { height: 240px; }

  /* Sur mesure */
  .sur-mesure__inner {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  /* Événements */
  .evenements__inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .evenements__img { height: 240px; }

  /* Avis */
  .avis__list { grid-template-columns: 1fr; }

  /* Accès */
  .acces__inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .acces__map-placeholder { height: 200px; }

  /* Footer */
  .footer__grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .footer__bottom { flex-direction: column; gap: 0.5rem; text-align: center; }
}

/* ===== REDUCED MOTION ===== */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { transition: none !important; animation: none !important; }
  html { scroll-behavior: auto; }
  .produits__img { transform: none !important; }
}
```

- [ ] **Step 2: Test at mobile width**

Open `landing/index.html` in browser. Use DevTools to resize to 375px width. Verify:
- Nav collapses to burger
- Hero stacks (text above photo)
- Bandeau is 2×2 grid
- All 2-col sections stack vertically
- Footer stacks to single column

- [ ] **Step 3: Commit**

```bash
git add landing/css/styles.css
git commit -m "feat: responsive — mobile breakpoints for all sections"
```

---

## Task 15: Final Polish — Spacing, Section Padding, Page Flow

**Files:**
- Append: `landing/css/styles.css`
- Minor: `landing/index.html` (add `id` attributes to sections if missing)

- [ ] **Step 1: Verify section IDs are correct**

Check that these `id` attributes exist in `landing/index.html`:
- `id="accueil"` on hero section
- `id="produits"` on produits section
- `id="sur-mesure"` on sur-mesure section
- `id="evenements"` on evenements section
- `id="contact"` on acces section

These are anchor targets for nav links — confirm nav links (`href="#produits"` etc.) match.

- [ ] **Step 2: Add final polish CSS**

Append to `landing/css/styles.css`:

```css
/* ===== POLISH ===== */

/* Smooth nav anchor offset (accounts for fixed nav height) */
[id] { scroll-margin-top: 80px; }

/* Section spacing consistency */
.bandeau + .valeurs { padding-top: 5rem; }

/* Campagne visual order swap on mobile (text first) */
@media (max-width: 980px) {
  .campagne__inner { direction: ltr; }
  .campagne__visual { order: 2; }
  .campagne__text  { order: 1; }
}

/* Produits last tile full-width on 2-col mobile grid */
@media (max-width: 980px) {
  .produits__tile:last-child { grid-column: span 2; max-width: 200px; margin: 0 auto; }
}
```

- [ ] **Step 3: Full scroll-through check**

Open `landing/index.html`. Scroll through every section top to bottom and verify:
1. Nav is visible and fixed ✓
2. Hero — heading, CTAs, photo ✓
3. Dark bandeau with 4 gold stats ✓
4. 3 value cards ✓
5. 5 product tiles with hover zoom ✓
6. Dark campaign section ✓
7. Sur mesure form ✓
8. Events section with fountain photo ✓
9. 3 testimonial cards ✓
10. Contact info + map placeholder ✓
11. Footer with newsletter ✓
12. No "démo", "concept", "provisoire" text anywhere ✓

- [ ] **Step 4: Commit**

```bash
git add landing/index.html landing/css/styles.css
git commit -m "feat: polish — scroll offset, section spacing, campagne mobile order"
```

---

## Self-Review Notes

- All 9 wireframe sections are covered (Tasks 3–11 + 12 for footer)
- Nav anchor links match section IDs
- Both logo uses covered: natural on light nav (Task 2), inverted on dark footer (Task 12)
- `chocolate-fountain.jpg` used in events (Task 9), `chocolate-hero.jpg` used everywhere else
- No "(démo)" text in any task
- Form submit buttons say "Envoyer ma demande" and "S'inscrire" — no demo labels
- Reduced motion media query included (Task 14)
- Skip link for accessibility included (Task 2)
- `scroll-margin-top` for fixed nav offset included (Task 15)
